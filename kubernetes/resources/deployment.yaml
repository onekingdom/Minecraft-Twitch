apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamwizard-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onekigndom-backend
  template:
    metadata:
      annotations:
        # linkerd.io/inject: enabled  # This annotation enables automatic injection
      labels:
        app: streamwizard-backend
    spec:
      containers:
        - name: streamwizard-backend
          image: ghcr.io/onekingdom/onekingdom-bot:455ce24dcf5e032f753622b2a68cfe4157ecd08a
          ports:
            - containerPort: 80
          envFrom:
            - secretRef:
                name: onekingdom-backend
